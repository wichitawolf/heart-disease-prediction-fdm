# Django settings
DEBUG=True
SECRET_KEY=replace-with-a-strong-secret-key
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (SQLite by default)
DJANGO_DB_ENGINE=django.db.backends.sqlite3
DJANGO_DB_NAME=config/db.sqlite3
DJANGO_DB_USER=
DJANGO_DB_PASSWORD=
DJANGO_DB_HOST=
DJANGO_DB_PORT=

# Static/Media (served locally in development)
STATIC_URL=/static/
MEDIA_URL=/media/

# Email (optional)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
